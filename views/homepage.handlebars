
<h1 id="title">home page</h1> 
{{#if logged_in}}
{{#if message}}
{{message}}</h1>
  {{/if}}
    {{/if}}
<div id="container">

    <div id="left-page">

        <div class="home-button-home">
            <button class="button"type="home-button">Home</button>
        </div>
        <div class="home-button">
            <button class="button"type="post-button">Post</button>
        </div>
        <div class="home-button">
            <button class="button"type="profile-button">Profile</button>
        </div>
    </div>

    <div id="middle-page">
        <section>
        {{#each posts as |post|}}
        {{> _posts}}
        {{/each}}
        </section>
    </div>

    <div id="right-page">
    {{!-- <input class="search-box" type="text" placeholder="search tweets" name="email" id="email" required> --}}
    {{!-- <button class="search-button" type="search-button">Search</button> --}}
    <section>
    <form action="/api/search" method="post">
      <input type="text" name="search" placeholder="Search...">
      <button id="search-button" type="submit">Search</button>
    </form>
    
    {{> _searchResults}}
    
    </section>
    <section>
        {{#each trendingPosts as |post|}}
        {{> _trending}}
        {{/each}}
    </section>
    </div>
</div>


