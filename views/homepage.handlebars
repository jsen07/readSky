
<h1 id="title">
{{#if message}}
{{message}}</h1>
  {{/if}}
<div id="container">

    <div id="left-page">

        <div class="home-button">
            <button class="button-home"><a href="/">Home</a></button>
        </div>
        <div class="home-button">
            <button class="button-home"><a href="/api/search">Explore</a></button>
        </div>
        {{#if logged_in}}
        <div class="home-button">
            <button class="button-home">Post</button>
        </div>
        <div class="home-button">
           <button class="button-home"><a href="/api/profile/{{user_id}}">Profile</a></button>
        </div>
          {{/if}}
    </div>

    <div id="middle-page">
      
        {{#each posts as |post|}}
        {{> _posts}}
    {{!-- {{#if logged_in}} --}}
        <div class="comment-section">
         <a id="post{{post.id}}" class="add-c"> Add a comment </a>
         <div class="comment-wrapper">
         <form class="add-comment">
        <p>Post Text:</p>
    <textarea id="text-comment" name="text" rows="4" cols="50"></textarea>
    <br>

</form>
    <button class="com-submit" id="comment-sbm">Submit</button>
    </div>
     </div>
       {{!-- {{/if}} --}}
           {{/each}}
    </div>

    <div id="right-page">
    {{!-- <input class="search-box" type="text" placeholder="search tweets" name="email" id="email" required> --}}
    {{!-- <button class="search-button" type="search-button">Search</button> --}}
    <h2>Trending Posts</h2>
    <section class="home-posts">
        {{#each trendingPosts as |post|}}
        {{> _trending}}
        {{/each}}
    </section>
    </div>
</div>


