<!-- search-results.handlebars -->

{{!-- {{#if searchResults.length}}
  <ul>
    {{#each searchResults as |result|}} --}}
      <li>
        <a href="/api/profile/{{id}}">
        <h2>{{username}}</h2>
        </a>
      </li>
    {{!-- {{/each}}
  </ul>
{{else}}
  <p>No results found.</p>
{{/if}} --}}


